<h1>ログインユーザーの投稿一覧</h1>

<% @post.each do |post| %>
  <%= image_tag image %>
  <%= post.user.name %>
  <%= post.user.user_id %>
  <%= post.location_name %>
  <%= post.address %>
  <%= post.memo %>

<% end %>