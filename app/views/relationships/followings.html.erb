<h1>フォロー一覧</h1>
<p><%= @user.followings.count %>人</p>

<% if @users.exists? %>
  <% @users.each do |user| %>
    <%= image_tag user.profile_image, size: "300x200" %>
    <p><%= user.name %></p>
    <p><%= user.account_id %></p>
  <% end %>
<% else %>
  <p>ユーザーはいません</p>
<% end %>
